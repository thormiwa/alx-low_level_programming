#include "holberton.h"

/**
 * _strlen - Entry point.
 * @s: string whose length should be returned
 * Return: Length of the string 
 */

int _strlen(char *s)
{
  int chars;
  
  for (chars =0; s[chars] != '\0'; chars++)
    {
      {
	return (chars);
}
